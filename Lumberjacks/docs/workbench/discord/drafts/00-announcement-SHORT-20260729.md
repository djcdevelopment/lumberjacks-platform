*Meta: the short-form announcement (supersedes the long draft — zero chance anyone reads
that much text). Paste everything below the divider.*

---

**The Workbench is open: https://am4.tail8e749c.ts.net/workbench**

I paused the deep netcode work at a clean checkpoint and spent the time on something you can actually touch: four tools that already run, each with an honest status and its own thread below. Nothing here is owed by anyone — this is infrastructure for the work this community already does.

**Run today:**
- **Quest picker** — your guild's real quest tracker becomes a check-off page the mod reads
- **ComfyStewardView** — point it at a world save, get the map: builds, portals, who owns what
- **Community telemetry** — live server stats, aggregates only (no names or positions — tested, not promised)
- **Steam join flow** — invite → Steam sign-in → mod-pack zip with your setup baked in

**Ground rules:**
- Feedback goes in the tool's thread, not DMs — replies land in batches, ~2×/week (solo operator)
- The game server isn't open yet; the *tooling* is — and two pruned pieces are up for revival if you want a real project to claim

Pick a thread below and run something. — Derek
