## Lab overlay for the Harmony patch-load A/B benchmark (runbook-patchload-ab-benchmark.md).
## Staged via Invoke-HeadlessValheimLab.ps1 -Action refresh -ConfigPath <this file>.
## Partial on purpose: BepInEx fills every unlisted key with its shipped default on boot,
## so this stays pinned to the one non-default the experiment needs.

[Perf]

perfPatchLoadRollupEnabled = true
